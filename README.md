# My dotfiles

```sh
git clone git@github.com:johantell/dotfiles.git
cd ./dotfiles

# Install brew dependencies
brew bundle

# Run rcrc to create symlinks to home folder
RCRC=$HOME/dotfiles/rcrc rcup
```
